/*
Задача 1.
Написать программу, выводящую все значения 
беззнаковой переменной типа char в консоль. 
Запрещено использовать что-либо, кроме самой переменной, 
одного цикла и вывода в поток в теле цикла.
*/

#include <iostream>

int main()
{
	unsigned char ch = 0;
	do{
		std::cout << int(ch) << std::endl;
		++ch;
	} while (int(ch)!=0);
}
